# Diagnosis playbook

Each diagnosis `probable_causes[].code` maps to a remediation note.

Browse codes with:

```bash
uv run eleanity playbook MISSING_ASSISTANT_TURN_TOKEN
```

HTML reports also surface playbook actions when causes are present.
